export enum BOOKING_STATUS{
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
    COMPLETED = 'completed'
}

export enum PAYMENT_STATUS{
    PENDING = 'pending',
    COMPLETED = 'completed',
    FAILED = 'failed'
}